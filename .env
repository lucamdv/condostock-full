# --- CONFIGURAÇÃO DO BANCO DE DADOS (POSTGRES) ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=condostock

# --- CONFIGURAÇÃO DO BACKEND (NESTJS / PRISMA) ---
# Atenção: O host aqui deve ser 'db' (nome do serviço no docker), não localhost.
DATABASE_URL="postgresql://user:password@db:5432/condostock?schema=public"

# Chave para criptografar os tokens de login (pode inventar qualquer frase)
JWT_SECRET="segredo_super_seguro_do_condominio_2025"

# --- CONFIGURAÇÃO DO FRONTEND (VITE / REACT) ---
# URL da API para o navegador acessar
VITE_API_URL="http://localhost:3000"